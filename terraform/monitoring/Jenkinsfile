pipeline {
    agent any

    stages {

        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/Ml1727/devops-ultimate.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t devops-app -f terraform/monitoring/Dockerfile .'
            }
        }

        stage('Run Container') {
            steps {
                sh '''
                docker rm -f devops-container || true
                docker run -d -p 5000:5000 --name devops-container devops-app
                '''
            }
        }
    }
}

